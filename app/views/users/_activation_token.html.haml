.alert{class: "activation-token-panel alert-#{token.redeemed ? 'warning' : 'info'}"}
  %p.code{class: "code-#{token.redeemed ? 'redeemed' : 'valid'}"}
    = token.token
  - if token.redeemed
    %p.redeem_date
      = 'Redeemed on:' + token.redeemed_on.to_s
